<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd">

	<!--读取外部配置文件 .properties =>value="${}	需要引入：xmlns:context="http://www.springframework.org/schema/context"命名空间
		<context:property-placeholder location="classpath:jdbc.properties,classpath:... , ..."/>
	-->
<!--	<context:property-placeholder location="classpath:jdbc.properties"/>-->
<context:property-placeholder location="classpath:jdbc.properties"/>
	<bean id="dataSource" class="com.alibaba.druid.pool.DruidDataSource">
		<property name="url" value="${practice.url}"/>
		<property name="driverClassName" value="${practice.driver}"/>
		<property name="username" value="${practice.username}"/>
		<property name="password" value="${practice.password}"/>
	</bean>

	<bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
		<property name="dataSource" ref="dataSource"/>
	</bean>


	<bean id="student" class="com.practice.model.Student" scope="prototype"/>

</beans>